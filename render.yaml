services:
  - type: web
    name: dashboard-ct-log
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    disk:
      name: dashboard-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
